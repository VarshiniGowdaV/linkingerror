# Suppose you have these files:
# main.c, utils.c (where utils.c contains some functions)

# Compiling only main.c
gcc main.c -o my_program
# Linking error: undefined reference to functions in utils.c
